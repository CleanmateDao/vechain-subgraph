# Copy to `.env` (same folder as docker-compose.yml) and fill in your RPC URLs.
#
# Used by `docker-compose.yml` to map manifest `network:` values to chain RPCs:
# - vechain-testnet -> $VECHAIN_TESTNET_RPC
# - vechain-mainnet -> $VECHAIN_MAINNET_RPC

# Example (QuickNode / Ankr / your own node):
VECHAIN_TESTNET_RPC=https://YOUR_TESTNET_RPC_URL
VECHAIN_MAINNET_RPC=https://YOUR_MAINNET_RPC_URL

# Optional: override local Postgres credentials (defaults shown)
POSTGRES_USER=graph-node
POSTGRES_PASSWORD=let-me-in
POSTGRES_DB=graph-node


